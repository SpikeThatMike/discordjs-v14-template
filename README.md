# discordjsv14 simple class handler
- custom slash commands cooldowns - default 3s
- built in reload function - unloads all events & commands then loads
- intellisene works for custom properties on client obj
